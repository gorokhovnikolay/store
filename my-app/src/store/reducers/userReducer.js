const initialState = {
	id: null,
	login: null,
	roleId: null,
	session: null,
};

export const userReducer = (state = initialState, { type, payload }) => {
	switch (type) {
		default:
			return state;
	}
};
